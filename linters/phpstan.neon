includes:
    - ../vendor/szepeviktor/phpstan-wordpress/extension.neon

parameters:
    level: 5
    excludePaths:
        - ../vendor/*
    ignoreErrors:
        # Unreachable code after parent::__construct() - false positive
        - '#Unreachable statement - code above always terminates#'
